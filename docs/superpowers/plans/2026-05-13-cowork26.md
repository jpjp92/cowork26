# Cowork26 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 소규모 팀이 브라우저에서 실시간 공동 편집할 수 있는 구글 시트 스타일 스프레드시트 서비스를 구축한다.

**Architecture:** React 19 + Vite 프론트엔드에서 FortuneSheet로 스프레드시트 UI를 제공하고, Vercel Functions로 CRUD API를 구성한다. 셀 데이터는 Supabase PostgreSQL에 저장하고, 실시간 협업은 Supabase Realtime broadcast를 브라우저에서 직접 구독해 처리한다.

**Tech Stack:** React 19, Vite, TypeScript, FortuneSheet, Vercel Functions, Supabase (PostgreSQL + Auth + Realtime), Tailwind CSS

---

## 파일 구조 맵

```
collab-sheets/
├── api/
│   ├── _lib/
│   │   └── supabase.ts          Supabase Service Key 클라이언트 (서버 전용)
│   └── sheets/
│       ├── index.ts             GET 목록 / POST 생성
│       └── [id]/
│           ├── cells.ts         GET 조회 / PUT 저장
│           └── members.ts       POST 초대
├── src/
│   ├── lib/
│   │   └── supabase.ts          Supabase Anon 클라이언트 (브라우저용)
│   ├── hooks/
│   │   ├── useAuth.ts           로그인/로그아웃/세션
│   │   ├── useSheets.ts         시트 목록 CRUD
│   │   ├── useCells.ts          셀 로드/저장
│   │   └── useRealtime.ts       Supabase broadcast 구독
│   ├── components/
│   │   ├── AuthForm.tsx         이메일 로그인/회원가입 폼
│   │   ├── SheetList.tsx        시트 목록 + 생성 버튼
│   │   ├── SheetEditor.tsx      FortuneSheet 래퍼
│   │   └── InviteModal.tsx      공동 작업자 초대 모달
│   ├── App.tsx                  라우팅 (로그인 → 목록 → 편집)
│   ├── main.tsx
│   └── index.css
├── supabase/
│   └── migrations/
│       └── 001_init.sql         테이블 생성 SQL
├── .env.local
├── .env.example
├── vercel.json
├── vite.config.ts
├── tailwind.config.js
├── postcss.config.js
└── package.json
```

---

## Task 1: 프로젝트 스캐폴딩

**Files:**
- Create: `package.json`
- Create: `vite.config.ts`
- Create: `tailwind.config.js`
- Create: `postcss.config.js`
- Create: `tsconfig.json`
- Create: `vercel.json`
- Create: `.env.example`
- Create: `src/main.tsx`
- Create: `src/index.css`
- Create: `index.html`

- [ ] **Step 1: Vite + React 19 + TypeScript 초기화**

```bash
cd /home/jpjp92/devs/github/collab-sheets
npm create vite@latest . -- --template react-ts
npm install
```

- [ ] **Step 2: 핵심 패키지 설치**

```bash
npm install @fortune-sheet/react @fortune-sheet/core
npm install @supabase/supabase-js
npm install react-router-dom
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p
```

- [ ] **Step 3: `tailwind.config.js` 설정**

```js
/** @type {import('tailwindcss').Config} */
export default {
  content: ['./index.html', './src/**/*.{ts,tsx}'],
  theme: { extend: {} },
  plugins: [],
}
```

- [ ] **Step 4: `src/index.css` 설정**

```css
@tailwind base;
@tailwind components;
@tailwind utilities;
```

- [ ] **Step 5: `vite.config.ts` 설정**

```ts
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  server: { port: 5173 },
})
```

- [ ] **Step 6: `vercel.json` 설정 (Functions 라우팅)**

```json
{
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
```

- [ ] **Step 7: `.env.example` 작성**

```
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_KEY=
```

- [ ] **Step 8: 개발 서버 실행 확인**

```bash
npm run dev
```

Expected: `http://localhost:5173` 에서 Vite 기본 페이지 확인

- [ ] **Step 9: 커밋**

```bash
git add .
git commit -m "feat: 프로젝트 초기 스캐폴딩 (Vite + React 19 + Tailwind)"
```

---

## Task 2: Supabase DB 마이그레이션

**Files:**
- Create: `supabase/migrations/001_init.sql`

> **사전 작업**: Supabase 대시보드(supabase.com)에서 새 프로젝트를 생성하고 URL, anon key, service key를 `.env.local`에 기록해 둔다.

- [ ] **Step 1: `.env.local` 생성**

```bash
cp .env.example .env.local
# 실제 값 입력
```

```
VITE_SUPABASE_URL=https://xxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...
SUPABASE_SERVICE_KEY=eyJ...
```

- [ ] **Step 2: 마이그레이션 SQL 작성**

`supabase/migrations/001_init.sql`:

```sql
-- 사용자 프로필 (auth.users 트리거로 자동 생성)
CREATE TABLE profiles (
  id           UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
  display_name TEXT,
  avatar_url   TEXT,
  created_at   TIMESTAMPTZ DEFAULT now()
);

-- auth.users 신규 가입 시 profiles 자동 생성
CREATE OR REPLACE FUNCTION handle_new_user()
RETURNS TRIGGER LANGUAGE plpgsql SECURITY DEFINER AS $$
BEGIN
  INSERT INTO profiles (id, display_name)
  VALUES (NEW.id, NEW.raw_user_meta_data->>'display_name');
  RETURN NEW;
END;
$$;

CREATE TRIGGER on_auth_user_created
  AFTER INSERT ON auth.users
  FOR EACH ROW EXECUTE FUNCTION handle_new_user();

-- 스프레드시트 문서
CREATE TABLE sheets (
  id         UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  title      TEXT NOT NULL DEFAULT '제목 없음',
  owner_id   UUID REFERENCES profiles(id) ON DELETE SET NULL,
  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now()
);

-- 시트별 공동 작업자
CREATE TABLE sheet_members (
  sheet_id  UUID REFERENCES sheets(id) ON DELETE CASCADE,
  user_id   UUID REFERENCES profiles(id) ON DELETE CASCADE,
  role      TEXT CHECK (role IN ('editor', 'viewer')) DEFAULT 'editor',
  joined_at TIMESTAMPTZ DEFAULT now(),
  PRIMARY KEY (sheet_id, user_id)
);

-- 셀 데이터
CREATE TABLE cells (
  id         UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  sheet_id   UUID REFERENCES sheets(id) ON DELETE CASCADE,
  row        INT NOT NULL,
  col        INT NOT NULL,
  value      TEXT,
  formula    TEXT,
  updated_by UUID REFERENCES profiles(id) ON DELETE SET NULL,
  updated_at TIMESTAMPTZ DEFAULT now(),
  UNIQUE (sheet_id, row, col)
);

-- RLS 활성화
ALTER TABLE profiles ENABLE ROW LEVEL SECURITY;
ALTER TABLE sheets ENABLE ROW LEVEL SECURITY;
ALTER TABLE sheet_members ENABLE ROW LEVEL SECURITY;
ALTER TABLE cells ENABLE ROW LEVEL SECURITY;

-- profiles: 본인만 조회/수정
CREATE POLICY "profiles_self" ON profiles
  FOR ALL USING (auth.uid() = id);

-- sheets: 오너 또는 멤버만 조회
CREATE POLICY "sheets_select" ON sheets
  FOR SELECT USING (
    owner_id = auth.uid() OR
    EXISTS (SELECT 1 FROM sheet_members WHERE sheet_id = id AND user_id = auth.uid())
  );

CREATE POLICY "sheets_insert" ON sheets
  FOR INSERT WITH CHECK (owner_id = auth.uid());

CREATE POLICY "sheets_update" ON sheets
  FOR UPDATE USING (owner_id = auth.uid());

CREATE POLICY "sheets_delete" ON sheets
  FOR DELETE USING (owner_id = auth.uid());

-- sheet_members: 관련 시트 멤버만 조회
CREATE POLICY "members_select" ON sheet_members
  FOR SELECT USING (
    EXISTS (SELECT 1 FROM sheets WHERE id = sheet_id AND owner_id = auth.uid())
    OR user_id = auth.uid()
  );

-- cells: 시트 멤버만 접근
CREATE POLICY "cells_select" ON cells
  FOR SELECT USING (
    EXISTS (
      SELECT 1 FROM sheet_members
      WHERE sheet_id = cells.sheet_id AND user_id = auth.uid()
    ) OR EXISTS (
      SELECT 1 FROM sheets
      WHERE id = cells.sheet_id AND owner_id = auth.uid()
    )
  );

CREATE POLICY "cells_upsert" ON cells
  FOR ALL USING (
    EXISTS (
      SELECT 1 FROM sheet_members
      WHERE sheet_id = cells.sheet_id AND user_id = auth.uid() AND role = 'editor'
    ) OR EXISTS (
      SELECT 1 FROM sheets
      WHERE id = cells.sheet_id AND owner_id = auth.uid()
    )
  );
```

- [ ] **Step 3: Supabase SQL Editor에서 실행**

Supabase 대시보드 → SQL Editor → 위 SQL 전체 붙여넣기 → Run

- [ ] **Step 4: 테이블 생성 확인**

Supabase 대시보드 → Table Editor에서 `profiles`, `sheets`, `sheet_members`, `cells` 4개 테이블 확인

- [ ] **Step 5: 커밋**

```bash
git add supabase/
git commit -m "feat: Supabase DB 스키마 및 RLS 정책 추가"
```

---

## Task 3: Supabase 클라이언트 & Auth 설정

**Files:**
- Create: `src/lib/supabase.ts`
- Create: `api/_lib/supabase.ts`
- Create: `src/hooks/useAuth.ts`
- Create: `src/components/AuthForm.tsx`

- [ ] **Step 1: 브라우저용 Supabase 클라이언트**

`src/lib/supabase.ts`:

```ts
import { createClient } from '@supabase/supabase-js'

export const supabase = createClient(
  import.meta.env.VITE_SUPABASE_URL,
  import.meta.env.VITE_SUPABASE_ANON_KEY
)
```

- [ ] **Step 2: 서버용 Supabase 클라이언트 (Vercel Functions)**

`api/_lib/supabase.ts`:

```ts
import { createClient } from '@supabase/supabase-js'

export const supabaseAdmin = createClient(
  process.env.SUPABASE_URL!,
  process.env.SUPABASE_SERVICE_KEY!
)
```

- [ ] **Step 3: useAuth 훅**

`src/hooks/useAuth.ts`:

```ts
import { useEffect, useState } from 'react'
import { Session } from '@supabase/supabase-js'
import { supabase } from '../lib/supabase'

export function useAuth() {
  const [session, setSession] = useState<Session | null>(null)
  const [loading, setLoading] = useState(true)

  useEffect(() => {
    supabase.auth.getSession().then(({ data }) => {
      setSession(data.session)
      setLoading(false)
    })

    const { data: { subscription } } = supabase.auth.onAuthStateChange((_event, session) => {
      setSession(session)
    })

    return () => subscription.unsubscribe()
  }, [])

  const signIn = (email: string, password: string) =>
    supabase.auth.signInWithPassword({ email, password })

  const signUp = (email: string, password: string, displayName: string) =>
    supabase.auth.signUp({
      email,
      password,
      options: { data: { display_name: displayName } },
    })

  const signOut = () => supabase.auth.signOut()

  return { session, loading, signIn, signUp, signOut }
}
```

- [ ] **Step 4: AuthForm 컴포넌트**

`src/components/AuthForm.tsx`:

```tsx
import { useState } from 'react'
import { useAuth } from '../hooks/useAuth'

export default function AuthForm() {
  const { signIn, signUp } = useAuth()
  const [mode, setMode] = useState<'login' | 'signup'>('login')
  const [email, setEmail] = useState('')
  const [password, setPassword] = useState('')
  const [displayName, setDisplayName] = useState('')
  const [error, setError] = useState('')

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault()
    setError('')
    const { error } =
      mode === 'login'
        ? await signIn(email, password)
        : await signUp(email, password, displayName)
    if (error) setError(error.message)
  }

  return (
    <div className="min-h-screen flex items-center justify-center bg-gray-50">
      <form
        onSubmit={handleSubmit}
        className="bg-white p-8 rounded-xl shadow-md w-full max-w-sm space-y-4"
      >
        <h1 className="text-2xl font-bold text-center">Cowork26</h1>
        {mode === 'signup' && (
          <input
            className="w-full border rounded px-3 py-2 text-sm"
            placeholder="이름"
            value={displayName}
            onChange={e => setDisplayName(e.target.value)}
            required
          />
        )}
        <input
          className="w-full border rounded px-3 py-2 text-sm"
          placeholder="이메일"
          type="email"
          value={email}
          onChange={e => setEmail(e.target.value)}
          required
        />
        <input
          className="w-full border rounded px-3 py-2 text-sm"
          placeholder="비밀번호"
          type="password"
          value={password}
          onChange={e => setPassword(e.target.value)}
          required
        />
        {error && <p className="text-red-500 text-sm">{error}</p>}
        <button
          type="submit"
          className="w-full bg-blue-600 text-white py-2 rounded hover:bg-blue-700 text-sm font-medium"
        >
          {mode === 'login' ? '로그인' : '회원가입'}
        </button>
        <p className="text-center text-sm text-gray-500">
          {mode === 'login' ? '계정이 없으신가요?' : '이미 계정이 있으신가요?'}{' '}
          <button
            type="button"
            className="text-blue-600 underline"
            onClick={() => setMode(mode === 'login' ? 'signup' : 'login')}
          >
            {mode === 'login' ? '회원가입' : '로그인'}
          </button>
        </p>
      </form>
    </div>
  )
}
```

- [ ] **Step 5: 개발 서버에서 렌더링 확인**

```bash
npm run dev
```

`src/App.tsx` 에서 `<AuthForm />` 임시 렌더링 후 로그인 폼이 표시되는지 확인

- [ ] **Step 6: 커밋**

```bash
git add src/lib/ src/hooks/useAuth.ts src/components/AuthForm.tsx api/_lib/
git commit -m "feat: Supabase 클라이언트 초기화 및 이메일 Auth 구현"
```

---

## Task 4: Vercel Functions — 시트 CRUD API

**Files:**
- Create: `api/sheets/index.ts`
- Create: `api/sheets/[id]/cells.ts`
- Create: `api/sheets/[id]/members.ts`

- [ ] **Step 1: Vercel Functions 의존성 설치**

```bash
npm install -D @vercel/node
```

- [ ] **Step 2: `api/sheets/index.ts` (GET 목록 / POST 생성)**

```ts
import type { VercelRequest, VercelResponse } from '@vercel/node'
import { supabaseAdmin } from '../_lib/supabase'

export default async function handler(req: VercelRequest, res: VercelResponse) {
  const token = req.headers.authorization?.replace('Bearer ', '')
  if (!token) return res.status(401).json({ error: 'Unauthorized' })

  const { data: { user }, error: authError } = await supabaseAdmin.auth.getUser(token)
  if (authError || !user) return res.status(401).json({ error: 'Unauthorized' })

  if (req.method === 'GET') {
    const { data, error } = await supabaseAdmin
      .from('sheets')
      .select('id, title, created_at, updated_at, owner_id')
      .or(`owner_id.eq.${user.id},sheet_members.user_id.eq.${user.id}`)
      .order('updated_at', { ascending: false })

    if (error) return res.status(500).json({ error: error.message })
    return res.json(data)
  }

  if (req.method === 'POST') {
    const { title } = req.body as { title?: string }
    const { data, error } = await supabaseAdmin
      .from('sheets')
      .insert({ title: title ?? '제목 없음', owner_id: user.id })
      .select()
      .single()

    if (error) return res.status(500).json({ error: error.message })

    // 오너를 sheet_members에도 editor로 추가
    await supabaseAdmin.from('sheet_members').insert({
      sheet_id: data.id,
      user_id: user.id,
      role: 'editor',
    })

    return res.status(201).json(data)
  }

  return res.status(405).json({ error: 'Method Not Allowed' })
}
```

- [ ] **Step 3: `api/sheets/[id]/cells.ts` (GET 조회 / PUT 저장)**

```ts
import type { VercelRequest, VercelResponse } from '@vercel/node'
import { supabaseAdmin } from '../../_lib/supabase'

export default async function handler(req: VercelRequest, res: VercelResponse) {
  const token = req.headers.authorization?.replace('Bearer ', '')
  if (!token) return res.status(401).json({ error: 'Unauthorized' })

  const { data: { user }, error: authError } = await supabaseAdmin.auth.getUser(token)
  if (authError || !user) return res.status(401).json({ error: 'Unauthorized' })

  const sheetId = req.query.id as string

  // 접근 권한 확인
  const { data: member } = await supabaseAdmin
    .from('sheet_members')
    .select('role')
    .eq('sheet_id', sheetId)
    .eq('user_id', user.id)
    .single()

  if (!member) return res.status(403).json({ error: 'Forbidden' })

  if (req.method === 'GET') {
    const { data, error } = await supabaseAdmin
      .from('cells')
      .select('row, col, value, formula, updated_by, updated_at')
      .eq('sheet_id', sheetId)

    if (error) return res.status(500).json({ error: error.message })
    return res.json(data)
  }

  if (req.method === 'PUT') {
    if (member.role !== 'editor') return res.status(403).json({ error: 'Read only' })

    const { row, col, value, formula } = req.body as {
      row: number
      col: number
      value: string
      formula?: string
    }

    const { data, error } = await supabaseAdmin
      .from('cells')
      .upsert(
        { sheet_id: sheetId, row, col, value, formula: formula ?? null, updated_by: user.id },
        { onConflict: 'sheet_id,row,col' }
      )
      .select()
      .single()

    if (error) return res.status(500).json({ error: error.message })
    return res.json(data)
  }

  return res.status(405).json({ error: 'Method Not Allowed' })
}
```

- [ ] **Step 4: `api/sheets/[id]/members.ts` (POST 초대)**

```ts
import type { VercelRequest, VercelResponse } from '@vercel/node'
import { supabaseAdmin } from '../../_lib/supabase'

export default async function handler(req: VercelRequest, res: VercelResponse) {
  if (req.method !== 'POST') return res.status(405).json({ error: 'Method Not Allowed' })

  const token = req.headers.authorization?.replace('Bearer ', '')
  if (!token) return res.status(401).json({ error: 'Unauthorized' })

  const { data: { user }, error: authError } = await supabaseAdmin.auth.getUser(token)
  if (authError || !user) return res.status(401).json({ error: 'Unauthorized' })

  const sheetId = req.query.id as string

  // 오너만 초대 가능
  const { data: sheet } = await supabaseAdmin
    .from('sheets')
    .select('owner_id')
    .eq('id', sheetId)
    .single()

  if (!sheet || sheet.owner_id !== user.id) return res.status(403).json({ error: 'Forbidden' })

  const { email, role } = req.body as { email: string; role: 'editor' | 'viewer' }

  // 이메일로 사용자 찾기
  const { data: invitee } = await supabaseAdmin
    .from('profiles')
    .select('id')
    .eq('id',
      (await supabaseAdmin.auth.admin.listUsers()).data.users.find(u => u.email === email)?.id ?? ''
    )
    .single()

  if (!invitee) return res.status(404).json({ error: '사용자를 찾을 수 없습니다' })

  const { data, error } = await supabaseAdmin
    .from('sheet_members')
    .upsert({ sheet_id: sheetId, user_id: invitee.id, role }, { onConflict: 'sheet_id,user_id' })
    .select()
    .single()

  if (error) return res.status(500).json({ error: error.message })
  return res.status(201).json(data)
}
```

- [ ] **Step 5: Vercel 로컬 dev로 Functions 동작 확인**

```bash
npx vercel dev
```

```bash
# 테스트 (토큰은 Supabase 로그인 후 획득)
curl -X GET http://localhost:3000/api/sheets \
  -H "Authorization: Bearer YOUR_TOKEN"
```

Expected: `[]` (빈 배열) 반환

- [ ] **Step 6: 커밋**

```bash
git add api/
git commit -m "feat: Vercel Functions 시트/셀/멤버 CRUD API 구현"
```

---

## Task 5: 시트 목록 UI (SheetList)

**Files:**
- Create: `src/hooks/useSheets.ts`
- Create: `src/components/SheetList.tsx`
- Modify: `src/App.tsx`

- [ ] **Step 1: useSheets 훅**

`src/hooks/useSheets.ts`:

```ts
import { useEffect, useState } from 'react'
import { supabase } from '../lib/supabase'

export interface Sheet {
  id: string
  title: string
  owner_id: string
  created_at: string
  updated_at: string
}

async function authHeader() {
  const { data } = await supabase.auth.getSession()
  return { Authorization: `Bearer ${data.session?.access_token}` }
}

export function useSheets() {
  const [sheets, setSheets] = useState<Sheet[]>([])
  const [loading, setLoading] = useState(true)

  const fetchSheets = async () => {
    const headers = await authHeader()
    const res = await fetch('/api/sheets', { headers })
    const data = await res.json()
    setSheets(data)
    setLoading(false)
  }

  useEffect(() => { fetchSheets() }, [])

  const createSheet = async (title: string) => {
    const headers = await authHeader()
    const res = await fetch('/api/sheets', {
      method: 'POST',
      headers: { ...headers, 'Content-Type': 'application/json' },
      body: JSON.stringify({ title }),
    })
    const sheet = await res.json()
    setSheets(prev => [sheet, ...prev])
    return sheet as Sheet
  }

  return { sheets, loading, createSheet, refetch: fetchSheets }
}
```

- [ ] **Step 2: SheetList 컴포넌트**

`src/components/SheetList.tsx`:

```tsx
import { useState } from 'react'
import { Sheet, useSheets } from '../hooks/useSheets'

interface Props {
  onOpen: (sheet: Sheet) => void
}

export default function SheetList({ onOpen }: Props) {
  const { sheets, loading, createSheet } = useSheets()
  const [title, setTitle] = useState('')

  const handleCreate = async () => {
    if (!title.trim()) return
    const sheet = await createSheet(title.trim())
    setTitle('')
    onOpen(sheet)
  }

  if (loading) return <div className="p-8 text-gray-400">불러오는 중...</div>

  return (
    <div className="max-w-2xl mx-auto p-8">
      <h2 className="text-xl font-bold mb-6">내 시트</h2>
      <div className="flex gap-2 mb-6">
        <input
          className="flex-1 border rounded px-3 py-2 text-sm"
          placeholder="새 시트 이름"
          value={title}
          onChange={e => setTitle(e.target.value)}
          onKeyDown={e => e.key === 'Enter' && handleCreate()}
        />
        <button
          onClick={handleCreate}
          className="bg-blue-600 text-white px-4 py-2 rounded text-sm hover:bg-blue-700"
        >
          만들기
        </button>
      </div>
      <ul className="space-y-2">
        {sheets.map(sheet => (
          <li
            key={sheet.id}
            onClick={() => onOpen(sheet)}
            className="border rounded px-4 py-3 cursor-pointer hover:bg-gray-50 flex justify-between items-center"
          >
            <span className="font-medium">{sheet.title}</span>
            <span className="text-xs text-gray-400">
              {new Date(sheet.updated_at).toLocaleDateString('ko-KR')}
            </span>
          </li>
        ))}
        {sheets.length === 0 && (
          <li className="text-gray-400 text-sm text-center py-8">
            시트가 없습니다. 새 시트를 만들어보세요.
          </li>
        )}
      </ul>
    </div>
  )
}
```

- [ ] **Step 3: App.tsx에 라우팅 연결**

`src/App.tsx`:

```tsx
import { useState } from 'react'
import { useAuth } from './hooks/useAuth'
import AuthForm from './components/AuthForm'
import SheetList from './components/SheetList'
import { Sheet } from './hooks/useSheets'

export default function App() {
  const { session, loading, signOut } = useAuth()
  const [activeSheet, setActiveSheet] = useState<Sheet | null>(null)

  if (loading) return <div className="min-h-screen flex items-center justify-center text-gray-400">로딩 중...</div>
  if (!session) return <AuthForm />

  return (
    <div className="min-h-screen bg-gray-50">
      <header className="bg-white border-b px-6 py-3 flex justify-between items-center">
        <span className="font-bold text-lg">Cowork26</span>
        <div className="flex gap-4 items-center">
          {activeSheet && (
            <button onClick={() => setActiveSheet(null)} className="text-sm text-gray-500 hover:text-gray-700">
              ← 목록
            </button>
          )}
          <button onClick={signOut} className="text-sm text-red-500 hover:text-red-700">로그아웃</button>
        </div>
      </header>
      <main>
        {activeSheet
          ? <div className="p-4 text-gray-400">시트 에디터 준비 중... (Task 6)</div>
          : <SheetList onOpen={setActiveSheet} />
        }
      </main>
    </div>
  )
}
```

- [ ] **Step 4: 시트 목록 동작 확인**

```bash
npx vercel dev
```

로그인 → 시트 만들기 → 목록에 나타나는지 확인

- [ ] **Step 5: 커밋**

```bash
git add src/hooks/useSheets.ts src/components/SheetList.tsx src/App.tsx
git commit -m "feat: 시트 목록 UI 및 생성 기능 구현"
```

---

## Task 6: FortuneSheet 에디터 연동

> **주의**: Task 7 (`useRealtime.ts`) 을 먼저 생성해야 이 태스크의 `SheetEditor.tsx` 임포트 에러가 없음. Task 7 → Task 6 순서로 진행.

**Files:**
- Create: `src/hooks/useCells.ts`
- Create: `src/components/SheetEditor.tsx`
- Modify: `src/App.tsx`

- [ ] **Step 1: useCells 훅 (셀 로드/저장)**

`src/hooks/useCells.ts`:

```ts
import { useEffect, useState, useCallback } from 'react'
import { supabase } from '../lib/supabase'

export interface CellData {
  row: number
  col: number
  value: string
  formula?: string
}

async function authHeader() {
  const { data } = await supabase.auth.getSession()
  return { Authorization: `Bearer ${data.session?.access_token}` }
}

export function useCells(sheetId: string) {
  const [cells, setCells] = useState<CellData[]>([])
  const [loading, setLoading] = useState(true)

  useEffect(() => {
    const fetchCells = async () => {
      const headers = await authHeader()
      const res = await fetch(`/api/sheets/${sheetId}/cells`, { headers })
      const data = await res.json()
      setCells(data)
      setLoading(false)
    }
    fetchCells()
  }, [sheetId])

  const saveCell = useCallback(async (cell: CellData) => {
    const headers = await authHeader()
    await fetch(`/api/sheets/${sheetId}/cells`, {
      method: 'PUT',
      headers: { ...headers, 'Content-Type': 'application/json' },
      body: JSON.stringify(cell),
    })
  }, [sheetId])

  const updateLocalCell = useCallback((cell: CellData) => {
    setCells(prev => {
      const idx = prev.findIndex(c => c.row === cell.row && c.col === cell.col)
      if (idx >= 0) {
        const next = [...prev]
        next[idx] = cell
        return next
      }
      return [...prev, cell]
    })
  }, [])

  return { cells, loading, saveCell, updateLocalCell }
}
```

- [ ] **Step 2: FortuneSheet 데이터 포맷 변환 유틸**

`src/lib/fortune.ts`:

```ts
import { CellData } from '../hooks/useCells'

export function cellsToFortune(cells: CellData[]) {
  const celldata: Record<string, unknown>[] = cells.map(c => ({
    r: c.row,
    c: c.col,
    v: {
      v: c.value,
      m: c.value,
      f: c.formula ?? undefined,
      ct: { fa: 'General', t: 'g' },
    },
  }))

  return [{
    name: 'Sheet1',
    celldata,
    row: 100,
    column: 26,
  }]
}

export function fortuneToCell(r: number, c: number, v: Record<string, unknown>): CellData {
  return {
    row: r,
    col: c,
    value: String(v?.m ?? v?.v ?? ''),
    formula: typeof v?.f === 'string' ? v.f : undefined,
  }
}
```

- [ ] **Step 3: SheetEditor 컴포넌트**

`src/components/SheetEditor.tsx`:

```tsx
import { useRef, useEffect } from 'react'
import { Workbook } from '@fortune-sheet/react'
import '@fortune-sheet/react/dist/index.css'
import { useCells } from '../hooks/useCells'
import { useRealtime } from '../hooks/useRealtime'
import { cellsToFortune, fortuneToCell } from '../lib/fortune'
import { Sheet } from '../hooks/useSheets'

interface Props {
  sheet: Sheet
}

export default function SheetEditor({ sheet }: Props) {
  const { cells, loading, saveCell, updateLocalCell } = useCells(sheet.id)
  const workbookRef = useRef<{ applyOp: (op: unknown) => void }>(null)

  // Realtime으로 수신한 셀 변경을 FortuneSheet에 반영
  useRealtime(sheet.id, (cell) => {
    updateLocalCell(cell)
    // FortuneSheet 직접 업데이트는 onChange로 처리 (리렌더링)
  })

  if (loading) return <div className="p-8 text-gray-400">셀 데이터 불러오는 중...</div>

  return (
    <div style={{ height: 'calc(100vh - 56px)' }}>
      <Workbook
        ref={workbookRef}
        data={cellsToFortune(cells)}
        onChange={async (data) => {
          const sheet = data[0]
          if (!sheet?.celldata) return
          // FortuneSheet onChange는 전체 celldata를 반환 — 마지막 변경만 저장
          // 실제로는 onCellUpdate 이벤트를 쓰는 것이 더 정확하나 버전 호환성 확인 필요
        }}
        onCellUpdateBefore={async (r, c, value) => {
          const cell = fortuneToCell(r, c, value as Record<string, unknown>)
          await saveCell(cell)
          updateLocalCell(cell)
          return true
        }}
        showToolbar={true}
        showFormulaBar={true}
        showSheetTabs={false}
      />
    </div>
  )
}
```

- [ ] **Step 4: App.tsx에 SheetEditor 연결**

`src/App.tsx` 에서 `activeSheet` 조건부 렌더링 수정:

```tsx
import SheetEditor from './components/SheetEditor'

// main 부분
{activeSheet
  ? <SheetEditor sheet={activeSheet} />
  : <SheetList onOpen={setActiveSheet} />
}
```

- [ ] **Step 5: 동작 확인**

```bash
npx vercel dev
```

시트 열기 → 셀 입력 → 새로고침 후 데이터 유지 확인

- [ ] **Step 6: 커밋**

```bash
git add src/hooks/useCells.ts src/lib/fortune.ts src/components/SheetEditor.tsx src/App.tsx
git commit -m "feat: FortuneSheet 에디터 연동 및 셀 저장/불러오기 구현"
```

---

## Task 7: 실시간 협업 (Supabase Realtime)

**Files:**
- Create: `src/hooks/useRealtime.ts`

- [ ] **Step 1: useRealtime 훅**

`src/hooks/useRealtime.ts`:

```ts
import { useEffect, useRef } from 'react'
import { supabase } from '../lib/supabase'
import { CellData } from './useCells'

export function useRealtime(sheetId: string, onCellChange: (cell: CellData) => void) {
  const callbackRef = useRef(onCellChange)
  callbackRef.current = onCellChange

  useEffect(() => {
    const channel = supabase.channel(`sheet:${sheetId}`)

    channel
      .on('broadcast', { event: 'cell-update' }, ({ payload }) => {
        callbackRef.current(payload as CellData)
      })
      .subscribe()

    return () => { supabase.removeChannel(channel) }
  }, [sheetId])

  const broadcast = async (cell: CellData) => {
    await supabase.channel(`sheet:${sheetId}`).send({
      type: 'broadcast',
      event: 'cell-update',
      payload: cell,
    })
  }

  return { broadcast }
}
```

- [ ] **Step 2: SheetEditor에서 broadcast 연결**

`src/components/SheetEditor.tsx` 의 `onCellUpdateBefore` 수정:

```tsx
// useRealtime에서 broadcast 받아서 saveCell 후 broadcast 전송
const { broadcast } = useRealtime(sheet.id, (cell) => {
  updateLocalCell(cell)
})

// onCellUpdateBefore 안에서:
const cell = fortuneToCell(r, c, value as Record<string, unknown>)
await saveCell(cell)
await broadcast(cell)
updateLocalCell(cell)
```

- [ ] **Step 3: 실시간 협업 동작 확인**

1. 브라우저 두 개 열기 (각각 다른 계정으로 로그인)
2. 같은 시트 접속
3. 한쪽에서 셀 입력 → 다른 쪽에서 실시간 반영 확인

- [ ] **Step 4: 커밋**

```bash
git add src/hooks/useRealtime.ts src/components/SheetEditor.tsx
git commit -m "feat: Supabase Realtime broadcast로 실시간 공동 편집 구현"
```

---

## Task 8: 공동 작업자 초대 UI

**Files:**
- Create: `src/components/InviteModal.tsx`
- Modify: `src/App.tsx` (헤더에 초대 버튼 추가)

- [ ] **Step 1: InviteModal 컴포넌트**

`src/components/InviteModal.tsx`:

```tsx
import { useState } from 'react'
import { supabase } from '../lib/supabase'

interface Props {
  sheetId: string
  onClose: () => void
}

export default function InviteModal({ sheetId, onClose }: Props) {
  const [email, setEmail] = useState('')
  const [role, setRole] = useState<'editor' | 'viewer'>('editor')
  const [status, setStatus] = useState<'idle' | 'loading' | 'success' | 'error'>('idle')
  const [errorMsg, setErrorMsg] = useState('')

  const handleInvite = async () => {
    setStatus('loading')
    const { data } = await supabase.auth.getSession()
    const res = await fetch(`/api/sheets/${sheetId}/members`, {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        Authorization: `Bearer ${data.session?.access_token}`,
      },
      body: JSON.stringify({ email, role }),
    })
    if (res.ok) {
      setStatus('success')
    } else {
      const err = await res.json()
      setErrorMsg(err.error ?? '오류가 발생했습니다')
      setStatus('error')
    }
  }

  return (
    <div className="fixed inset-0 bg-black/40 flex items-center justify-center z-50">
      <div className="bg-white rounded-xl p-6 w-full max-w-sm space-y-4 shadow-xl">
        <h3 className="font-bold text-lg">공동 작업자 초대</h3>
        <input
          className="w-full border rounded px-3 py-2 text-sm"
          placeholder="이메일"
          type="email"
          value={email}
          onChange={e => setEmail(e.target.value)}
        />
        <select
          className="w-full border rounded px-3 py-2 text-sm"
          value={role}
          onChange={e => setRole(e.target.value as 'editor' | 'viewer')}
        >
          <option value="editor">편집자</option>
          <option value="viewer">뷰어</option>
        </select>
        {status === 'success' && <p className="text-green-600 text-sm">초대 완료!</p>}
        {status === 'error' && <p className="text-red-500 text-sm">{errorMsg}</p>}
        <div className="flex gap-2">
          <button
            onClick={handleInvite}
            disabled={status === 'loading'}
            className="flex-1 bg-blue-600 text-white py-2 rounded text-sm hover:bg-blue-700 disabled:opacity-50"
          >
            {status === 'loading' ? '초대 중...' : '초대'}
          </button>
          <button
            onClick={onClose}
            className="flex-1 border py-2 rounded text-sm hover:bg-gray-50"
          >
            닫기
          </button>
        </div>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: App.tsx 헤더에 초대 버튼 추가**

```tsx
import InviteModal from './components/InviteModal'

// 상태 추가
const [showInvite, setShowInvite] = useState(false)

// 헤더 버튼 추가 (activeSheet 있을 때)
{activeSheet && (
  <button
    onClick={() => setShowInvite(true)}
    className="text-sm text-blue-600 hover:text-blue-800"
  >
    초대
  </button>
)}

// 모달 렌더링
{showInvite && activeSheet && (
  <InviteModal sheetId={activeSheet.id} onClose={() => setShowInvite(false)} />
)}
```

- [ ] **Step 3: 초대 기능 동작 확인**

편집 중 헤더 "초대" 버튼 → 이메일 입력 → 초대 완료 메시지 확인

- [ ] **Step 4: 커밋**

```bash
git add src/components/InviteModal.tsx src/App.tsx
git commit -m "feat: 공동 작업자 이메일 초대 모달 구현"
```

---

## Task 9: Vercel 배포

**Files:**
- Modify: `.env.local` (확인용)

- [ ] **Step 1: Vercel CLI로 프로젝트 연결**

```bash
npx vercel link
```

프롬프트에서 `jpjp92` 팀, 새 프로젝트 이름 `cowork26` 선택

- [ ] **Step 2: 환경 변수 설정**

```bash
npx vercel env add VITE_SUPABASE_URL
npx vercel env add VITE_SUPABASE_ANON_KEY
npx vercel env add SUPABASE_URL
npx vercel env add SUPABASE_SERVICE_KEY
```

각각 Preview, Production 모두 설정

- [ ] **Step 3: Preview 배포**

```bash
npx vercel
```

Expected: `https://cowork26-xxxx.vercel.app` URL 출력

- [ ] **Step 4: 배포된 URL에서 전체 흐름 확인**

1. 회원가입 → 로그인
2. 시트 생성 → 셀 입력 → 새로고침 후 데이터 유지
3. 두 브라우저에서 실시간 편집 확인

- [ ] **Step 5: Production 배포**

```bash
npx vercel --prod
```

- [ ] **Step 6: 최종 커밋**

```bash
git add .
git commit -m "feat: Vercel 배포 설정 완료"
git push origin main
```

---

## 완료 기준 체크리스트

- [ ] 이메일 회원가입 / 로그인 / 로그아웃 동작
- [ ] 시트 생성 및 목록 조회
- [ ] FortuneSheet 셀 편집 및 DB 저장
- [ ] 새로고침 후 셀 데이터 복원
- [ ] 두 사용자가 같은 시트에서 실시간 동시 편집
- [ ] 공동 작업자 이메일 초대
- [ ] Vercel Production 배포 완료
